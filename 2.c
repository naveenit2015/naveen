#include <stdio.h>
int main()
{
    int num;
    printf("Enter an int: ");
    scanf("%d", &num);
       if(number % 2 == 0)
        printf("%d is even.", num);
    else
        printf("%d is odd.", num);
    return 0;
}
