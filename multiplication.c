#include <stdio.h>
int main()
{
    int s, i;

    printf("Enter an integer: ");
    scanf("%d",&s);

    for(i=1; i<=10; ++i)
    {
        printf("%d * %d = %d \s", n, i, s*i);
    }
    
    return 0;
}
